# SLS / HotelKontrolle Zürich credentials.
#
# Copy this file to `hoko-cli/.env` and fill in your login.
# `hoko-cli/.env` is gitignored AND covered by a Claude deny rule in
# `.claude/settings.local.json` so neither git nor I can read it.
#
# Login URL: https://www.hotelkontrolle.zh.ch/HoKoDMZ/login.xhtml
SLS_USER=
SLS_PASS=
